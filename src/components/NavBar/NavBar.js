import "./NavBar.css";

import Logo from "../Logo/Logo";

const NavBar = () => {
  return (
    <nav className="navbar">
      <div className="navbar-content">
        <Logo />
      </div>
    </nav>
  );
};
export default NavBar;
